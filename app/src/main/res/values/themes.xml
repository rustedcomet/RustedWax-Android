<?xml version="1.0" encoding="utf-8"?>
<resources>
	<style name="Theme.RustedWax" parent="android:Theme.Material.NoActionBar">
		<!--
			Colour lives in `RustedWaxTheme`; this only stops the launch window
			from being white while Compose is still starting up. See
			`values/colors.xml`.
		-->
		<item name="android:windowBackground">@color/window_background</item>
	</style>
</resources>
